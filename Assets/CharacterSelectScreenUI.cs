using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterSelectScreenUI : MonoBehaviour
{
    private void OnEnable()
    {
        // TODO get GameData.characters and set them
        //  this can be done in an awake function on the other component for this screen actually since we just nbeed to create a little ui for them by loopin over them
    }
}
